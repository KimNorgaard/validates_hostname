require:
  - rubocop-rake
  - rubocop-rspec

AllCops:
  Exclude:
    - "bin/*"
    - "db/**/*"
    - "config/**/*"
    - "script/**/*"
    - "vendor/**/*"
    - "tmp/**/*"
    - "node_modules/**/*"

Metrics/MethodLength:
  Exclude:
    - "lib/validates_hostname.rb"

Metrics/AbcSize:
  Exclude:
    - "lib/validates_hostname.rb"

Metrics/CyclomaticComplexity:
  Exclude:
    - "lib/validates_hostname.rb"

Metrics/PerceivedComplexity:
  Exclude:
    - "lib/validates_hostname.rb"

Metrics/BlockLength:
  Exclude:
    - "spec/validates_hostname_spec.rb"
    - "validates_hostname.gemspec"
